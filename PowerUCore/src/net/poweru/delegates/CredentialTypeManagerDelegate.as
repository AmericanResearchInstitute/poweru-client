package net.poweru.delegates
{
	import mx.rpc.IResponder;
	
	public class CredentialTypeManagerDelegate extends BaseDelegate
	{
		public function CredentialTypeManagerDelegate(responder:IResponder)
		{
			super(responder, 'CredentialTypeManager');
		}
	}
}